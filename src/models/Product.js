class Product {
  constructor(baseUrl, id, title, description, price) {
    this.baseUrl = baseUrl;
    this.id = id;
    this.title = title;
    this.description = description;
    this.price = price;
  }
}

export default Product;
